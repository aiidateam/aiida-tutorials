language: python
python:
 - "2.7"
 - "3.6"

addons:
  apt:
    packages:
      - pandoc
      - texlive-full

install:
  - pip install --upgrade pip
  - pip install -r docs/requirements.txt

script: cd docs; make
