channels:
  - conda-forge
dependencies:
  - python=3.7
  # aiida environment
  - aiida-core==1.2.1
  - aiida-core.services==1.2.1
  # jupyter requirements
  - notebook==5.7.9
  # - jupyterlab
  # - jupyterhub-singleuser
  # displays an indication of how much resources your current notebook server and its children (kernels, terminals, etc) are using
  - nbresuse==0.3.6
  # - ipywidgets
  # to convert markdown files to notebooks
  - jupytext==1.5.0
  - myst-parser==0.8.2
